import 'package:flutter/material.dart';

class Aplicacao4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Help"),
      ),
    );
  }
}
