import 'package:flutter/material.dart';

class ajusteBebida extends StatelessWidget {
  const ajusteBebida({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
