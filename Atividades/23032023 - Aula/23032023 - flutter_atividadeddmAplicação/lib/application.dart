import 'package:flutter/material.dart';
import 'package:flutter_atividadeddm/application2.dart';

class Aplicacao extends StatelessWidget {
  const Aplicacao({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bem vindo ao conversor!'),
        centerTitle: true,
      ),
      );
  }
}
