import 'package:flutter/material.dart';

class Detalhes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fim')),
      body: ElevatedButton(
        child: const Text('Fim'),
        onPressed: () {},
      ),
    );
  }
}
