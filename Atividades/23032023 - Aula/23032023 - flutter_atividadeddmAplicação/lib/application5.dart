import 'package:flutter/material.dart';

class Aplicacao5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Fim"),
      ),
    );
  }
}
