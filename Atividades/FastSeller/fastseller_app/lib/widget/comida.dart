import 'package:flutter/material.dart';

class ajusteComida extends StatelessWidget {
  const ajusteComida({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
