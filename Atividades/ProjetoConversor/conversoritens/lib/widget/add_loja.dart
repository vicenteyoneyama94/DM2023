import 'package:flutter/material.dart';

class add_Loja extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Adicionar Loja'), actions: []),
    );
  }
}
